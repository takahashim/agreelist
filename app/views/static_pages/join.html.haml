.jumbotron
  .text-center
    %h2= current_user.name
    %h3= "@#{current_user.twitter}"
    %h3 Email:
    = form_for @individual do |f|
      = f.text_field :email, size: 30
      = f.submit "Send"
