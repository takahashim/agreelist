<script type="text/javascript">
  google.load("visualization", "1", {packages:["corechart"]});
  google.setOnLoadCallback(drawOccupationsChart);
  var occupations = [];
  occupations[0] = ['Occupation', 'link', 'Number'];
  <% @occupations_count.each_with_index do |occupation, index| %>
    <% name = occupation[0] %>
    <% count = occupation[1] %>
    occupations[<%= index + 1 %>] = ['<%= name.titleize %>', '<%= statement_path(@statement, occupation: name) %>', <%= count %>];
  <% end %>
</script>
